---
ruleId: "attr/media-presentational"
title: "Presentational attributes on media"
description: "Presentational attributes on img, object and embed are obsolete. Browsers apply them as low-priority hints. Move styling to CSS."
pubDate: "2026-09-21"
status: "avoid"
severity: "deprecated"
standardsBasis: "spec-obsolete"
detectability: "yes"
kind: "element"
scope: "body"
selector: "img[align], img[border], img[hspace], img[vspace], object[align], object[border], object[hspace], object[vspace], embed[align], embed[hspace], embed[vspace]"
fix: { op: "none" }
replacement: "Style the media in CSS (float, vertical-align, border, margin) and delete the attributes."
tags: ["attr", "legacy", "style"]
impacts: ["maintainability"]
related: ["attr/table-presentational"]
---

Eleven presentational attributes on `img`, `object` and `embed` are obsolete. `<img src="cover.jpg" alt="" align="left" hspace="10" border="1">` is how media was placed before CSS. The attributes aligned media with surrounding text and spaced it from neighbors. They still turn up in old pages and in HTML generated by older tools.

## Why avoid

They are obsolete. The HTML Standard lists all eleven spellings among the attributes that "are obsolete (though the elements are still part of the language), and must not be used by authors", with the instruction: "Use CSS instead." Section 16.3 still backs them with reflected IDL across the three interfaces, which is live machinery: the attributes keep working as presentational hints in the weakest place in the author cascade, where any stylesheet rule for the same property wins.

So deleting one repaints the media wherever no stylesheet rule covers the property. Each attribute either does nothing, because CSS overrides it, or sets a value the stylesheet never states. Either way, part of the page design lives in markup, where nobody editing the CSS will look for it. MDN files the four `img` spellings under deprecated attributes, each paired with its CSS property; its `embed` page documents the live attributes with none of the three among them.

## Use instead

Move the layout to CSS and drop the hints:

```html
<img src="cover.jpg" alt="Cover art">
```

```css
img {
  float: left;
  margin: 0 1rem 1rem 0;
  border: 1px solid #ccc;
  vertical-align: middle;
}
```

## Detectability

Complete detection. The rule reports each element once, however many of the eleven attributes it carries. It checks `img`, `object` and `embed`, and nothing else. There is no autofix: removing a live hint repaints the media, so the move to CSS needs a human look at the values.

## Resources

- [HTML Standard: Non-conforming features](https://html.spec.whatwg.org/multipage/obsolete.html#non-conforming-features): all eleven spellings are obsolete; use CSS instead.
- [MDN: `<img>`](https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/img): `align`, `border`, `hspace` and `vspace` sit under deprecated attributes, each paired with its CSS property.
- [MDN: `<embed>`](https://developer.mozilla.org/en-US/docs/Web/HTML/Reference/Elements/embed): documents the live attributes with none of the three among them.
