# Refuse to install on a Node version outside the "engines" range rather than
# warning. The whole toolchain assumes >=24 (native type stripping, styleText,
# fs.glob), so failing loudly here beats a confusing error later.
engine-strict=true
